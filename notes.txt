Function IsCellEmpty(Cell As Range) As Boolean
    ' Returns True if the cell is empty or contains only whitespace, False otherwise
    IsCellEmpty = True
    If Not IsEmpty(Cell) And Len(Trim(Cell.Value)) > 0 Then
        IsCellEmpty = False
    End If
End Function
